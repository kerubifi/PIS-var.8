version: '3.8'
services:
  db:
    image: postgres:15
    container_name: listing_db
    environment:
      POSTGRES_DB: listing_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
