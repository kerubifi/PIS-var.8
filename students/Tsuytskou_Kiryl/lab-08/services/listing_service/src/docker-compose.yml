version: '3.8'
services:
  db:
    image: postgres:15
    container_name: blog_db
    environment:
      POSTGRES_DB: longread_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
